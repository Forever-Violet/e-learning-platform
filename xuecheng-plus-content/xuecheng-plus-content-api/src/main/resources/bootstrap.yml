#微服务配置
spring:
  application:
    name: content-api #服务名
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://120.46.58.64:3306/xuecheng_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
#    username: root
#    password: ufo7338199
  cloud:
    nacos: # 服务注册相关配置
      server-addr: 120.46.58.64:8801
      discovery:
        namespace: dev
        group: xuecheng-plus-project
      config: # 配置文件相关信息
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        extension-configs: # 扩展配置
          - data-id: content-service-${spring.profiles.active}.yaml
            group: xuecheng-plus-project
            refresh: true
        shared-configs: # 共享配置
          - data-id: swagger-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true
          - data-id: logging-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true
  profiles:
    active: dev #环境名
# 日志文件配置路径
#logging:
#  config: classpath:log4j2-dev.xml
#  level:
#    com.alibaba.nacos: warn  #调整日志级别, 不然会一直输出无用日志

#swagger:
#  title: "学成在线内容管理系统"
#  description: "内容管理系统对课程相关信息进行管理"
#  base-package: com.xuecheng.content
#  enabled: true
#  version: 1.0.0
#server:
##  port: 63041
